//STRING GETS AND PUTS

#include<stdio.h>
#include<conio.h>
int main()
{
   char name[20];
   printf("\nenter the string=");
   //scanf("%s",&name);
   gets(name);
   puts(name);
   //printf("%s",name);
   getch();
}

