//PROGRAM TO SHOW USE OF SWITCH CASE
#include<stdio.h>
#include<conio.h>
int main()
{
    char m;
    printf("enter a character=");
    scanf("%c",&m);
    switch(m)
    {
      case 'a': printf("case is ankit");
     break;
      case 'b': printf("case is b");
     break;
      case 'e':printf("case is celina");
     break;
    default:
             printf("case does not exist");
      }
      getch();
      }
                       
