#include <bits/stdc++.h>

using namespace std;



int main() {
     bool c;
    int q;
    long long int a[26],b[26];

    cin >> q;
    for(int a0 = 0; a0 < q; a0++){
        char s1[10000],s2[1000];
        cin>>s1>>s2;
       sort(s1,sl.length());
        sort(s2,s2.length());
        if(c)
        {
            cout<<"YES"<<endl;
        }
        else
        {
            cout<<"NO"<<endl;
        }
    }

    return 0;
}
