#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin>>n;

    //n++;
    while(n--)
    {
    string str;
    cin>>str;
    //std::getline(std::cin,str);
    cout << str<< endl;
    }

    return 0;
}
